/* import { useState } from 'react'
import { Button, Modal, Popover, Typography  } from '@mui/material' */
/* import DatepickerComponent from './DatepickerComponent' */

const DatumKnapp = (props: any) => {

/*   const { id, dates, timeValues, handleDateChange, handleTimeChange } = props
  
  const [open, setOpen] = useState(false)
  const [anchorEl, setAnchorEl] = useState(null)

    const close = () => {
      setOpen(false)
    }

    const handleClick = (e) => {
      setAnchorEl(e.currentTarget)
      setOpen(!open)
    }

    return (
        <>
        <Button className='header-valj-datum-button' variant='outlined' onClick={e => handleClick(e)}>Välj datum</Button>
        <Modal
              open={open}
              onClose={() => close()}
              aria-labelledby="modal-modal-title"
              aria-describedby="modal-modal-description"
            >
              <Popover
                  id={id}
                  open={open}
                  anchorEl={anchorEl}
                  anchorOrigin={{
                    vertical: 'center',
                    horizontal: 'center',
                  }}
                  transformOrigin={{
                    vertical: 'center',
                    horizontal: 'center',
                  }}
                  onClose={() => close()}
                > 
         <Typography sx={{ p: 2 }}>Välj datum för upphämtning och avlämning.</Typography>
         <div>
             <DatepickerComponent dates={dates} timeValues={timeValues} handleDateChange={handleDateChange} handleTimeChange={handleTimeChange} onClose={handleDateChange} />
             </div>
         </Popover>
            </Modal>
            </>
    ) */
}


export default DatumKnapp